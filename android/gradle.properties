# 🔧 Memory settings (Optimized for Flutter + Kotlin)
# 4GB heap is optimal for most Flutter projects (was 8GB - too high!)
# 1GB metaspace is sufficient (was 4GB - excessive!)
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# 🎯 Kotlin Daemon settings (IMPORTANT - fixes daemon connection issues!)
kotlin.daemon.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m

# 🧠 Core Android settings
android.useAndroidX=true
android.enableJetifier=true

# ⚡ Performance tweaks
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.caching=true
org.gradle.vfs.watch=true
kotlin.incremental=true

# 💾 Faster dependency resolution
android.enableD8=true
android.useNewApkCreator=true
android.enableParallelJsonGen=true

# 🔥 Additional optimizations
kotlin.incremental.usePreciseJavaTracking=true
kotlin.caching.enabled=true
